/**
 * Enum que define as chaves usadas no Async Store.
 */
export enum AsyncStorageKeys {
  /**
   * Chave para a autenticação do usuário no Async Store.
   */
  Authentication = 'authentication',
}
